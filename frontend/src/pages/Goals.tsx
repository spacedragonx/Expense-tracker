import { Target } from "lucide-react";
import PageStub from "@/components/common/PageStub";

export default function Goals() {
  return (
    <PageStub
      title="Goals"
      description="Save toward specific targets and track contributions."
      icon={Target}
    />
  );
}
